image: node:latest

stages: 
- build


before_script:
 - pwd
 - cd src/juke-app

build:
 stage: build
 script:
  - npm install -g @angular/cli
  - npm update -g @angular/cli
  - npm run build
 artifacts:
  expire_in: 1 day
  paths:
   - dist/

after_script:
- echo "end CI"